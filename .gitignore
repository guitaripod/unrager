/target
*.log
.env
.env.*
/tests/fixtures/real_*.json

# Xcode / iOS build artifacts (native client lives in ios/)
ios/build/
ios/DerivedData/
ios/.build/
**/xcuserdata/
**/*.xcuserstate

# Generated by VHS shot-*.tape — the PNGs are promoted to assets/ on each run
demos/shots/

# Cloudflare Pages local dev (wrangler cache + transient deps for /site)
site/.wrangler/
site/node_modules/
