/target
*.log
.env
.env.*
/tests/fixtures/real_*.json

# Dioxus web bundle — generated by `just web` / CI. Regenerated on every
# release so we don't track it in the repo.
crates/unrager-app/dist/

# Dioxus CLI scratch
.dx/

# Generated by VHS shot-*.tape — the PNGs are promoted to assets/ on each run
demos/shots/
